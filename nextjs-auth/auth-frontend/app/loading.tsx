import { Loading as LoadingComponent } from "./components/Loading"

export default function Loading() {
  return (
    <section aria-label="loading container">
      <LoadingComponent />
    </section>
  )
}
